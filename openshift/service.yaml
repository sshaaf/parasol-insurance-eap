apiVersion: v1
kind: Service
metadata:
  name: parasol-insurance-eap
  labels:
    app: parasol-insurance-eap
spec:
  selector:
    app: parasol-insurance-eap
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
